# Galerie Photos - Dockerfile de développement
# À implémenter selon le langage choisi (Go, Rust, Python, etc.)

FROM alpine:latest

LABEL maintainer="Cloudity Team"
LABEL service="gallery-service"
LABEL version="0.1.0"

# Installation des dépendances de base
RUN apk add --no-cache ca-certificates

# Création du répertoire de travail
WORKDIR /app

# Port d'écoute
EXPOSE 8100

# Point d'entrée temporaire
CMD ["echo", "Galerie Photos - À implémenter"]
