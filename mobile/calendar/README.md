# Cloudity Calendar (mobile)

App Flutter **MVP** : connexion suite, liste agendas/événements (`GET /calendar/…`), paramètres, alignement web.

```bash
make run-mobile APP=Calendar
```

Édition et rappels natifs = prochaines itérations (voir `docs/produit/MOBILES.md`).
