# cloudity_ota_installer

Alias Dart déprécié. Le plugin natif est `hubera_ota_installer`
(canal `hubera_ota_installer` + canal legacy `cloudity_ota_installer`).

```dart
import 'package:cloudity_ota_installer/cloudity_ota_installer.dart';
// réexporte HuberaOtaInstaller (+ typedef CloudityOtaInstaller)
```
