name: cloudity_ota_installer
description: Alias déprécié — utilise hubera_ota_installer.
publish_to: "none"
version: 0.1.1

environment:
  sdk: ^3.11.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  hubera_ota_installer:
    path: ../hubera_ota_installer
