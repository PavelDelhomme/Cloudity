# Changelog — @cloudity/ui

## 0.1.0 — 2026-05-19

- Package initial : primitives extraites de `@cloudity/shared` (`PageLayout`, `Button`, `Card`, tables, `Badge`, `Input`, `Label`).
- Preset Tailwind `brand` + `Spinner`, `EmptyState`, `IconButton`.
- Catalogue admin `/4dm1n/dev/ui`.
