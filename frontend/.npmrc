# Cloudity frontend — npm defaults (FE-SEC-SUPPLY-02)
# npm 12 : scripts install bloqués sauf allowScripts dans package.json
# (esbuild approuvé). Rebuild : ./scripts/frontend/npm-ci-hardened.sh

fund=false
audit=false
engine-strict=false
