# Cloudity frontend — npm defaults (FE-SEC-SUPPLY-02)
# Ne pas activer ignore-scripts=true tant que esbuild/vite n’ont pas un
# rebuild explicite dans Makefile/Docker (sinon binaries manquants).
# Objectif suivant : ignore-scripts=true + `npm rebuild esbuild` allowlist.

fund=false
audit=false
engine-strict=false
