/** Réexport depuis la source unique `@cloudity/shared` (web / extension / mobile TS). */
export {
  APP_LABELS,
  CLOUDITY_APP_IDS,
  DEFAULT_USER_PREFERENCES,
  THEME_MODE_LABELS,
  USER_PREFERENCES_CACHE_KEY,
} from '@cloudity/shared'
export type {
  CloudityAppId,
  PassPreferences,
  ThemeMode,
  ThemePreferences,
  UserPreferencesV1,
} from '@cloudity/shared'
