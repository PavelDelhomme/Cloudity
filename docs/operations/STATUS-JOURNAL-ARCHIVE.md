# STATUS — Journal archivé

**Rôle** : historique détaillé **2026-05-12 → 2026-05-15** (sprint Pass J1–J7, infra, déploiement), extrait de la racine **`STATUS.md`** le **2026-05-16** pour éviter les doublons « Dernière mise à jour » en tête du fichier canonique.

**Fichier à jour pour le quotidien** : **[../../STATUS.md](../../STATUS.md)** (section *À faire maintenant* + une seule date).

---

### 2026-05-15 — **Anti-spam multi-couches + chiffrement mail (documentation)**. Demande utilisateur : aligner la vision (WAF → gateway → rate limit → auth → métier ; **séparé** du filtrage **SMTP/IMAP**), intégrer les pistes Perplexity/River/MLflow **sans** présenter comme « déjà en prod » ce qui ne l’est pas (ex. **pas** de Prometheus/Grafana dans le `docker-compose` actuel — **TR-06**). (Doc) **`docs/architecture/ANTI-SPAM-ET-ABUS.md`** : chemins HTTP vs MTA, Rspamd + Postfix/Dovecot en **AS-1** avant tout `antispam-service` ML (**AS-5**), réutilisation **Redis** existant avec préfixes de clés (éviter un second Redis « spam » tant que non nécessaire), `api-gateway` intelligent (rate limits par route, suite du travail déjà amorcé login/register), **fail2ban** côté **hôte VPS** (pas dans le conteneur app). (Doc) **`docs/securite/MAIL-CHIFFREMENT-ET-ANTI-SPAM.md`** : trois niveaux (secrets boîte AES-GCM **déjà** ; corps E2E S/MIME·OpenPGP **long terme** ; **Pass** ≠ MIME SMTP), principes **quarantaine / ham / pas de suppression silencieuse** pour ne pas bloquer l’envoi légitime. Croisements : **`SECURITE.md`**, **`SECURITE-DONNEES.md`**, **`SYNC-BACKLOG.md` § 0e**, **`DEV-VERIFICATION.md` § 5**, **`docs/README.md`**, **`BACKLOG.md`** (lignes **AS-0..AS-5**). **Pas de code nouveau** dans ce commit-doc : implémentation **Rspamd + M7** reste après stabilisation **Mail Core** / sprint Pass (cf. **VISION-SUITE**).

### 2026-05-13 (J7 sprint Pass — surfaces clientes manquantes notées + squelette extension Pass MV3 + placeholder Calendar) — **Réponse à la demande utilisateur** « créer les dossiers et projets pour l'extension navigateur Pass, l'app Linux Pass, les apps mobiles Calendar / Drive / Photos, et apps Linux desktop ; note-le bien ». Audit transversal réalisé : la matrice apps × plateformes vivait en silos (`MOBILES.md` couvrait mobile vs web, mais ni desktop Linux ni extension navigateur). (Doc) **`docs/produit/MULTI-PLATEFORME.md`** créé (215 lignes) : matrice complète **11 apps × 7 plateformes** avec statut ✅ / 🟡 scaffold / ❌ / ⛔ non pertinent ; § 3 stratégie d'ordre rentable (sprint Pass → post-20 mai → mois suivant) ; § 4 conventions de scaffolding (Flutter, extension MV3, lien `@cloudity/pass-crypto`) ; § 5 anti-patterns « 14 apps moitié-faites » + critère de sortie de scaffold (build + test fume + doc + STATUS daté) ; § 6 8 prochaines actions concrètes MP-01..MP-08 avec échéances. Lien depuis `MOBILES.md` (devient désormais « focus mobile vs web », la matrice transversale = MULTI-PLATEFORME.md). (Extension navigateur Pass) **`extensions/cloudity-pass/`** squelette MV3 livré (~700 lignes hors HTML/CSS) : `manifest.json` MV3 strict (CSP `default-src 'self'`, permissions minimales `storage / activeTab / scripting / alarms`, `web_accessible_resources: []`, **pas** de `host_permissions: <all_urls>` global), `package.json` `@cloudity/pass-extension v0.1.0` (deps `@cloudity/pass-crypto` via workspace + `esbuild` + `typescript` + `@types/chrome`), `tsconfig.json` strict, **build `scripts/build.mjs`** (esbuild 4 entrées : `background.js` / `content.js` / `popup/popup.js` / `options/options.js` + copie `manifest.json` + `popup/static/` + `options/static/` + warning explicite si `icons/` manquant), 4 modules TS (`src/background/index.ts` 165 l : service worker MV3, master key uniquement en mémoire — `zeroize` au lock + à la fermeture, auto-lock 5 min via `chrome.alarms` qui survit aux suspensions du worker, gestion typée des messages `unlock/lock/ping/status/save-gateway` ; `src/content/index.ts` 90 l : détection passive `input[type="password"]` + `autocomplete="username"`, badge discret quand le coffre est déverrouillé, ping background pour repousser l'auto-lock, **aucun champ rempli automatiquement** ; `src/popup/popup.ts` 130 l : déverrouillage maître / verrouillage manuel + countdown auto-lock live ; `src/options/options.ts` 40 l : configuration URL gateway). HTML/CSS popup et options en dark/light auto via `color-scheme`. **`tsc --noEmit` 0 erreur, `npm run build` produit `dist/` ~5 KB total**. README détaillé avec architecture + modèle de menace (CSP stricte, vault key qui ne sort jamais du worker, pas de `web_accessible_resources`, pas de `externally_connectable`) + roadmap MP-01..MP-08 + procédure de chargement Chrome locale. Pas d'icônes PNG embarquées (squelette) — le build affiche un avertissement clair. (Calendar mobile) **`mobile/calendar/`** placeholder livré : `README.md` 65 l qui explique pourquoi un placeholder (réservation d'emplacement sans exploser le scope sprint), pré-requis (backend `calendar-service` + page web `cloudity-web /app/calendar` à amorcer **avant**, cf. règle « web avant mobile » MOBILES.md § 0), procédure de scaffold (`flutter create --platforms=android,ios .`), conventions à respecter (réutiliser `cloudity_shared.Auth2FAClient` + clés `cloudity_suite_*` partagées Drive/Mail/Photos) ; `pubspec.yaml` 0.0.0+placeholder explicite, **pas de `flutter pub get`** tant que le scaffold complet n'est pas fait. (Note Mail) `mobile/mail/README.md` complété avec la matrice cibles plateformes (Android ✅ / iOS 🟡 jamais testé / Linux ❌ à scaffolder MP-03) + section sécurité 2FA mobile (rappel J7 ter parité). **Bilan** : aucun scope produit additionnel démarré, mais toutes les surfaces clientes attendues sont désormais **listées, datées, dotées d'un ID MP-xx, et prêtes à être prises en main**. Engagement : on ne dérape pas du sprint Pass — ces 8 items sont en L2/L3 explicitement.

### 2026-05-13 (J7 sprint Pass — parité 2FA mobile) — **Apps Dart Drive / Mail / Photos passées au même niveau de durcissement 2FA que le web**. Suite à demande utilisateur (« on fait un niveau bien plus élevé actuellement de sécurité, mais on n'a pas changé cela aussi pour les autres applications Dart en fait ? ») : audit constate que les 3 apps Flutter renvoyaient simplement *« Ce compte a la double authentification. Utilisez le web ou désactivez le 2FA »* sur `requires_2fa: true` — incompatible avec le durcissement web (URL capabilities + recovery codes + Passkeys). (Shared) **`mobile/cloudity_shared/lib/auth_2fa.dart`** (175 lignes) : exception métier `LoginRequires2FAException(email, tenantId, userId?)` (le mot de passe **n'est jamais conservé** car `/auth/2fa/verify` ne le requiert pas), client `Auth2FAClient.verify({email, tenantId, code})` → `Auth2FAResult(accessToken, refreshToken, expiresIn, usedRecoveryCode, recoveryCodes)`, helper `looksLikeRecoveryCode` aligné serveur (`recoverycodes.go`). `pubspec.yaml` bump 0.1.0 → 0.2.0 (ajout dep `http: ^1.2.2` + `test: ^1.25.7`). Export public via `cloudity_shared.dart`. **11 tests `dart test` verts** (TOTP/recovery détection, succès TOTP, succès avec recovery_codes inclus, 401 → exception lisible, code vide refusé *avant* appel réseau, 500 → exception, 200 sans `access_token` → exception). (Drive / Mail / Photos) **6 fichiers modifiés** : chaque `auth_api.dart` convertit `requires_2fa: true` → `LoginRequires2FAException` + nouvelle méthode `verify2FA(email, tenantId, code)` qui délègue à `Auth2FAClient` ; chaque `login_screen.dart` ajoute un état `_twoFactorRequired` qui bascule sur un formulaire dédié (`_build2FAForm`) avec un seul champ « Code 2FA » (TOTP 6 chiffres **ou** recovery 12 chars `XXXX-XXXX-XXXX`, détection automatique côté serveur), bouton « Annuler / changer de compte » qui réinitialise. Mail conserve son scan multi-gateway (`SessionStore.gatewayCandidates()`) — l'exception 2FA est attrapée dans la boucle pour mémoriser le bon `AuthApi`. Le mot de passe est **effacé du contrôleur** dès la bascule en mode 2FA. (Tests) `flutter analyze lib` : **0 issue** sur Drive / Mail / Photos / cloudity_shared. `flutter test` : Drive ✅, Mail ✅, Photos ✅. (Doc) `docs/securite/URL-CAPABILITIES.md` § 7 « Couverture mobile (apps Dart) — parité 2FA » : explique pourquoi les *capability URLs* restent web-only (vecteurs Referer/historique inexistants en Flutter), comment la parité serveur (validation TOTP + recovery + rate-limit) couvre suffisamment, et liste les durcissements futurs (TLS pinning, Passkey mobile via `local_auth`, auto-clear clipboard généralisé). Pass mobile **non concerné** : il ne fait pas de login direct, il consomme la session web. `admin_app` non concerné : squelette PoC sans login implémenté. **Bilan** : aucun trou de durcissement entre web et mobile sur la 2FA — un user qui active la 2FA via les nouvelles capability URLs peut désormais se reconnecter à Drive / Mail / Photos sans désactiver le 2FA.

### 2026-05-13 (J7 sprint Pass — refacto fichiers + doc services) — **Subdivision `webauthn.go` + nouvelle ref `docs/architecture/SERVICES.md`**. Suite à demande utilisateur (« le nom des services dans le tableau status ; à quoi sert `cloudity-redis-commander` ? »). (Doc) **`docs/architecture/SERVICES.md`** créé : référence par conteneur Docker (image, port host, profil, rôle), section dédiée aux outils dev (`cloudity-adminer` UI Postgres :6083 / `cloudity-redis-commander` UI Redis :6084 — basic auth `admin/admin`, profil `dev` uniquement, **NE TOURNENT PAS EN PROD**), explication de `db-migrate` qui apparaît `OK (job)` au tableau `make status`. Index `docs/README.md` mis à jour. STATUS.md § 4.2 : descriptions adminer/redis-commander étoffées (rôles concrets : inspecter refresh tokens, challenges WebAuthn, rate-limit login). (Refacto Go) `backend/auth-service/webauthn.go` (~780 lignes) **scindé** en 7 fichiers ≤ 235 lignes chacun : `webauthn.go` (config + service + RegisterRoutes, 136 l), `webauthn_user.go` (134 l, `webauthnUser` + handle ↔ id + `loadUser`), `webauthn_session.go` (50 l, Redis TTL 5 min usage unique), `webauthn_register.go` (118 l, `RegisterBegin/Finish` + `passkeyRegistrationOptions`), `webauthn_login.go` (235 l, `LoginBegin/Finish` regular + discoverable + `passkeyLoginOptions`), `webauthn_credentials.go` (154 l, `ListCredentials`/`DeleteCredential`/`persistCredential`/`bumpSignCount`), `webauthn_auth.go` (54 l, `requireAuthUser`). Tests Go ✅ 2.8 s — aucune signature publique modifiée.

### 2026-05-13 (J7 sprint Pass — durcissement post-soir) — **URL capabilities rotatives livrées (durcissement 2FA / settings)**. Suite à demande utilisateur (« la url de récupération 2FA doit être super sécurisée, hashée par utilisateur, regénérée régulièrement, sauf pour les liens de partage qui doivent rester stables ») : (Backend) `backend/auth-service/securetoken_hmac.go` + `securetoken_http.go` + `securetoken_test.go` — token capability HMAC-SHA-256 sur `(user_id, purpose, epoch 30 j)`, tronqué 128 bits, format `<epoch>.<base64url(hmac16)>`, **fenêtre coulissante** (epoch courant + précédent → tolérance ~60 j max). Whitelist de purposes (anti-rejeu inter-pages). Secret = `URL_TOKEN_SECRET` ≥32 oct **OU** dérivation déterministe `SHA-256("cloudity-url-tokens-v1:" || JWT_SECRET)` (fallback : pas de variable supplémentaire à câbler dans Portainer). Endpoints `GET /auth/security-paths` (Bearer) → `{ paths: { settings_security: { path, token, expires_at, rotates_at } }, issued_at, window_seconds }` avec `Cache-Control: no-store` + `Referrer-Policy: no-referrer` ; `POST /auth/security-paths/validate` (Bearer) → 200/403. Mode dégradé : 503 si aucun secret, le SPA retombe sur `/app/settings/canonical`. **9 tests Go verts** (issue + verify happy path / wrong user / wrong purpose / expired / sliding window / tampered / malformed / fallback JWT_SECRET / fail-closed sans secret). (Frontend) `useSecurePaths.ts` (React Query, `staleTime 30 min`, slug **jamais persisté**), `SettingsRedirect.tsx` (canonique → rotatif via `Navigate`), `SecureSettingsPage.tsx` (valide le token via `POST /auth/security-paths/validate`, redirige `/app/settings/canonical` si 403, **injecte `<meta name="referrer" content="no-referrer">`** pendant le montage). 5 nouveaux tests Vitest (`useSecurePaths.test.tsx` : redirige vers slug rotatif, repli silencieux 503, validation OK rend Settings, validation 403 redirige canonique, hook expose `isRotated=true`). (Migration) `infrastructure/postgresql/migrations/39-pass-share-tokens.sql` — table `pass_share_tokens` (token aléatoire 192 bits **hashé SHA-256**, `vault_id`/`item_id`/`created_by`/`expires_at`/`revoked_at`/`use_count`) ; squelette pour partage Pass stable révocable (UI + endpoints en L2/L3). (Doc) `docs/securite/URL-CAPABILITIES.md` — threat model (capture d'écran, Referer, historique, partage d'écran), modèle rotatif vs. modèle stable, matrice de mode dégradé, références croisées vers `securetoken.go` / `useSecurePaths.ts` / `PASS-CRYPTO.md` ; lien ajouté depuis `SECURITE.md` § 8. **Tests live** : auth-service Go ✅ 2.1 s, Vitest cloudity-web **283/283 pertinents** (1 flake pré-existant `vaultContext > auto-lock` qui passe en isolation, indépendant de ces changements). Endpoint `GET /auth/security-paths` confirmé live via `curl` → `401` + en-têtes `Cache-Control: no-store` + `Pragma: no-cache` corrects.

### 2026-05-13 (J7 sprint Pass — soir) — **J7 livré : mobile Pass Flutter LECTURE SEULE**. Squelette Flutter `mobile/pass/` (Android + Linux desktop) avec `cloudity_pass` v0.1.0. (Crypto Dart) Port `lib/pass_crypto.dart` du package web `@cloudity/pass-crypto` strictement interopérable : Argon2id (`cryptography` ^2.7.0, profils `mobileLow` 64 MiB t=3 p=2 / `mobileHigh` 128 MiB / `desktop` 256 MiB t=4 p=4 alignés sur `ARGON2ID_PROFILES` web), HKDF-SHA-256 (salt 32×0x00, label `cloudity-pass/v1/vault-key:` + vault_id), XChaCha20-Poly1305 (split tag 16 oct ↔ format `noble-ciphers` web qui colle `ct||tag`), CBOR (`cbor` ^6.3.1) → décodage `EnvelopeV1` complet (déchiffre `wrap` avec VK pour récupérer `IK_item`, puis `ct` avec `IK_item`, schema=1). `deriveUserSaltDeterministic` aligné sur `vaultContext.tsx` (préfixe `cloudity-pass:v1:user-salt:`). (UI Flutter) 5 écrans : `PassLoginScreen` (gateway/email/password/tenant) → `PassUnlockScreen` (mot de passe maître + sélecteur de profil Argon2id) → `PassVaultsScreen` (liste vaults + lock manuel + logout) → `PassItemsScreen` (recherche locale, déchiffrement par item, icône par type) → `PassItemDetailScreen` (champs masquables, copie auto-clear 30 s, type/schema affichés). `VaultController` central : MK uniquement en mémoire Dart (jamais `flutter_secure_storage` brut), `WidgetsBindingObserver` re-verrouille à chaque pause/inactive/detached, auto-lock 5 min d'inactivité, `zeroize` au lock et au dispose. `PassSessionStore` distinct des autres apps Cloudity (clés `cloudity_pass_*` — Pass ne partage pas la session car le coffre se reverrouille à chaque relance). (Tests) **21/21 verts** (`flutter test`, ~10 s) : 16 unitaires pass-crypto Dart (salt, base64url avec/sans padding, alphabet URL-safe, refus `EnvelopeV1` invalide, alignement `Argon2idParams` web/mobile, `HkdfLabels`, `zeroize`) + **5 cross-stack web→mobile** (`cross_stack_vector_test.dart`) qui rejouent le vecteur figé `frontend/packages/pass-crypto/src/__tests__/vectors.test.ts` : MK hex bit-à-bit identique (`46d34f0b75afe0…`), VK hex bit-à-bit identique (`bef6308f2247fa…`), déchiffrement de l'enveloppe figée → `{title:'Vector test', url, username, password, notes, tags:['vector']}` exact. **Garantie d'interop** : si une dépendance Dart change sa sortie ces tests cassent **avant** que les coffres deviennent illisibles. `flutter analyze` 0 issue. (E2E re-tests live) Stack docker compose toujours up : Playwright `e2e/pass.spec.ts` **4/4 verts** (8.8 s) + `e2e/webauthn.spec.ts` **2/2 verts** (2.2 s) — confirme que J5+J6 (recovery codes + Passkeys W2) n'ont pas régressé après J7. Édition mobile Pass = L2 (J+1..J+5 après la migration Proton). Prochaine étape : **J8** = migration Proton (20 mai).

### 2026-05-13 (J5 + J6 sprint Pass — après-midi) — **J5 et J6 livrés en avance**. (J5 backend) Migration **38** `recovery_codes` (`id`, `user_id INTEGER FK CASCADE`, `code_hash bcrypt cost 12`, `used_at`, `created_at` ; index partiel `WHERE used_at IS NULL` pour le lookup au login). Module `backend/auth-service/recovery_codes.go` : génération `XXXX-XXXX-XXXX` alphabet **sans caractères ambigus** (pas de `0/O/1/I/L`), 12 chars, **~59 bits d'entropie**, normalisation tirets/espaces, distinction TOTP-6 vs recovery-12 via `looksLikeRecoveryCode`, **timing-constant** `bcrypt.CompareHashAndPassword` sur tous les codes valides (pas de fuite « plus que 1 code restant »). Endpoint `POST /auth/2fa/recovery-codes/regenerate` (Bearer) + `GET /auth/2fa/recovery-codes/count` (UI warning ≤2). `Verify2FA` accepte un code de récupération **OU** un TOTP, marque `used_at` en transaction (anti-race), et **génère 10 codes à la 1ère activation** uniquement (transition `is_2fa_enabled` false→true). 5 tests Go (`recovery_codes_test.go` : format, unicité 100 itérations, normalisation, look-like, alphabet sans ambigu). (J5 passkeys W2) `webauthn.go` : `loadAdminUser` → `loadUser` (ouvert à tout user `is_active=true`), helpers `passkeyRegistrationOptions` (`RequireResidentKey: true` + `ResidentKey: required` + `UserVerification: preferred` + `Attestation: none`) et `passkeyLoginOptions`. Quota **5 passkeys/user**. Endpoints `POST /auth/webauthn/login/begin-discoverable` (utilise `BeginDiscoverableLogin` go-webauthn) + `/finish-discoverable` (résout `userHandle → user_id` via `userIDFromWebAuthnID`). `requireAdminUser` → `requireAuthUser` (renvoie `(uid, role)` ; chaque caller fait sa vérif). Tests Go : `TestRequireAuthUserAcceptsRegularUser`, `TestRequireAuthUserRejectsMissingBearer`, `TestRequireAuthUserDefaultsToUserRole`, `TestUserIDWebAuthnIDRoundTrip` (4 valeurs dont `MaxInt64`), `TestUserIDFromInvalidHandle`. (J6 frontend) `webauthn.ts` : `loginWithPasskeyDiscoverable` (vérifie `PublicKeyCredential.isConditionalMediationAvailable`, `mediation: 'conditional'`, `signal` AbortController). `LoginPage.tsx` refacto : (1) `autoComplete="username webauthn"` + `current-password webauthn` → déclenche la **Conditional UI** au focus du champ email — Proton Pass / iCloud Keychain / Bitwarden propose la passkey directement, (2) `useEffect` lance `loginWithPasskeyDiscoverable` au mount, navigue automatiquement si succès, silencieux sinon, (3) **étape 2FA** : si `requires_2fa` revient du backend, bascule sur un formulaire dédié qui accepte TOTP 6 chiffres OU code récup `XXXX-XXXX-XXXX`, toast spécifique « connexion via code de récupération — pense à régénérer ». `api.ts` : helpers `enable2FA`, `verify2FA`, `regenerateRecoveryCodes`, `countRecoveryCodes`. (J6 Settings) `pages/app/settings/PasskeysSection.tsx` (réutilise listPasskeys/registerPasskey/deletePasskey, compteur **X / 5**, désactive bouton à 5) + `RecoveryCodesSection.tsx` (compteur live, warning ≤2, régénération avec confirmation, affichage UNE fois en grid 2 col `select-all`, **« Tout copier » + « Imprimer »** popup avec date d'émission). `AppSettingsPage.tsx` : 3 sections (Session / Passkeys / 2FA). (Bug fixé) Volume bind `./backend/internalsec:/internalsec:cached` ajouté à `auth-service` + `api-gateway` dans `docker-compose.yml` (Air rebuild échouait sur `replace ../internalsec` car le dossier n'était pas monté). (E2E) Fixture login adapté pour 2 boutons (« Se connecter » exact + « passkey »), e2e/pass.spec.ts ciblé `getByRole('textbox')` (libellés URL/Utilisateur partagés avec boutons), e2e/webauthn.spec.ts tolère la Conditional UI qui authentifie avant le clic manuel. **Tests** : Go auth-service ✅ 2.0 s, Vitest cloudity-web **278/278 (3 skipped)**, Playwright **6/6** (4 Pass + 2 WebAuthn). Prochaine étape : **J7** (mobile Pass Flutter LECTURE SEULE 19 mai) puis **J8** (migration réelle 20 mai).

### 2026-05-13 (J4 sprint Pass — après-midi) — **J4 livré (import Proton JSON + TOTP client + e2e Playwright)**. (Parser) `protonImport.ts` : `parseProtonExport` + `convertProtonToCloudity` — refuse `encrypted: true`, tolère variations mineures du JSON Proton, mappe `login` → `ItemPlaintextV1` type `login` (champs `title`/`url`/`username`/`password`/`totpUri`), `note` → type `note`, autres types → `note` avec dump structuré + `protonOriginalType` dans `fields`. **8 tests Vitest** (`protonImport.test.ts`). (TOTP RFC 6238) `totp.ts` : `base32Decode`, `generateTotp` (Web Crypto HMAC SHA-1/256/512), `parseOtpauthUri`, `totpSecondsRemaining` — **16 tests** dont vecteurs **RFC 6238 Appendix B** (SHA-1 @ T=59/1111111109/1111111111, SHA-256 @ T=59). `TotpDisplay.tsx` : code rotatif + anneau SVG + copie auto-clear (TTL min(secondes restantes, 30 s)). `ItemEditor.tsx` : champ `totpUri` + affichage `TotpDisplay` si non vide. (Import UI) `ProtonImportDialog.tsx` : drop zone + file picker, aperçu par vault Proton, import dans le vault Cloudity sélectionné. `PassPage.tsx` : bouton « Importer Proton », `onProtonImport` avec **concurrence bornée 4** (Promise pool) pour chiffrer + `POST /pass/vaults/:id/items` en parallèle. (E2E) `e2e/pass.spec.ts` réécrit : déverrouillage maître (`PLAYWRIGHT_E2E_MASTER` ou défaut = mot de passe démo), création vault + 1 entrée, verrouillage manuel, import JSON synthétique **3 entrées** (dont 1 avec `totpUri` JBSWY3DPEHPK3PXP) + assert toast succès + 3 titres visibles. **Vitest cloudity-web : 278 verts | 3 skipped** (31 fichiers, ~12 s). Prochaine étape sprint : **J5** (2FA login web + codes de récupération + Passkeys user backend).

### 2026-05-13 (J2 sprint Pass — matin avancé) — **J2 + début J3 livrés en avance**. (J2) `frontend/packages/pass-crypto` v0.1.0 reçoit deux ajouts : (1) **vecteurs déterministes figés bit-à-bit** (`src/__tests__/vectors.test.ts`) pour Argon2id (MK 32 oct hex), HKDF-SHA-256 (VK 32 oct hex) et le format `EnvelopeV1` complet (CBOR + base64url) avec RNG xorshift32 seed `0xdeadc0de` — toute régression silencieuse d'une dépendance crypto (`hash-wasm`, `@noble/ciphers`, `@noble/hashes`, `cbor-x`) fait échouer le test **avant** que les coffres existants ne se cassent ; (2) **bench Argon2id par profil** (`scripts/bench-argon2.mjs`, cible `npm run bench:argon2 -w @cloudity/pass-crypto`) — warmup + médiane sur N itérations sur 4 profils (`test`, `desktop`, `mobile-high`, `mobile-low`), affiche écart aux cibles temps (1000 / 600 / 400 ms). Mesures laptop dev : desktop 924 ms (-8 %), mobile-high 378 ms, mobile-low 165 ms — pas de réajustement nécessaire (les vrais devices mobiles seront plus lents). Pass-crypto à **22/22 verts** (~257 ms). (J3 partie 1) **UI Pass web** complètement refactorée sous `frontend/apps/cloudity-web/src/pages/app/pass/` : `vaultContext.tsx` (master key 32 oct **uniquement** en mémoire React, jamais dans `localStorage` / `sessionStorage` ; auto-lock 5 min d'inactivité — listener `keydown`/`mousemove`/`touchstart` reset le timer ; zeroïsation `.fill(0)` au lock et au démontage ; salt utilisateur dérivé déterministe SHA-256(préfixe + `user_id`) — non secret par construction), `UnlockScreen.tsx` (saisie mot de passe maître + dérivation Argon2id côté client avec loader), `ItemEditor.tsx` (form login : titre/URL/utilisateur/mot de passe/notes, toggle visibilité, copie clipboard avec auto-clear, bouton générateur intégré, bouton supprimer), `clipboardAutoClear.ts` (`copyWithAutoClear` 30 s par défaut, n'écrase pas un copier-coller ultérieur de l'utilisateur via `navigator.clipboard.readText()`, fallback overwrite blind si permission refusée Firefox/Safari), `PassPage.tsx` réécrit avec `VaultProvider` + 2 colonnes (vaults / items) + recherche locale (titre/URL/utilisateur/notes) + déchiffrement local de chaque ciphertext via `decryptItemFromVault`. **API** : ajout `createVaultItem` / `updateVaultItem` / `deleteVaultItem` dans `src/api.ts` (le backend `passwords-service` POST `/pass/vaults/:id/items` + PUT/DELETE `/pass/items/:id` existait déjà avec RLS user_id). Tests `cloudity-web` : **254 verts | 3 skipped** (29 fichiers, ~12 s) — dont 11 nouveaux : `PassPage.test.tsx` (3 — JWT sans `user_id` → bloqué, JWT valide → écran de déverrouillage, mention auto-lock & Argon2id), `vaultContext.test.tsx` (3 — démarre locked, unlock + lock zeroïse, auto-lock après `AUTO_LOCK_AFTER_MS` avec `vi.useFakeTimers()` et `deriveMasterKey` mockée), `clipboardAutoClear.test.ts` (5 — écrit immédiat, efface après TTL, n'écrase pas un copier-coller ultérieur, `cancel()` annule, fallback overwrite blind si `readText()` lève). ESLint sans warning. Reste J3 : tests Playwright e2e sur le flux complet (déverrouillage + 1 item) — repoussé en marge de J4 pour tester en même temps que l'import Proton JSON.

### 2026-05-13 (J1 sprint Pass — fin nuit) — **Passkeys utilisateur + intégration password managers tiers ajoutées au sprint Pass (L1, J5-J6)**. Demande utilisateur 2026-05-13 nuit : permettre à **Proton Pass / Bitwarden / 1Password / iCloud Keychain** d'enregistrer le login Cloudity + la passkey, pour simplifier la connexion (Conditional UI au focus du champ email). État actuel audité : `backend/auth-service/webauthn.go` + `frontend/apps/cloudity-web/src/webauthn.ts` existent (Phase W1, lib `go-webauthn/webauthn v0.17.3`, table `webauthn_credentials` migr. 37) **mais** réservés admin (`if role != 'admin'` aux lignes 161/340/589) **et** sans `residentKey: required` (defaults `preferred` ⇒ les password managers tiers n'enregistrent pas la passkey, c'est le critère W3C `discoverable credential`) **et** sans Conditional UI au login. Trois changements sur ~1,5 à 2 j (J5-J6 en parallèle 2FA pour mutualiser les chantiers DB / Settings) : (a) ouverture enrôlement aux users non-admin avec quota 5/user, (b) `residentKey: required` + `userVerification: preferred` côté `BeginRegistration`, (c) endpoint `POST /auth/webauthn/login/begin-discoverable` + Conditional UI front (`autocomplete="username webauthn"` + `navigator.credentials.get({ mediation: 'conditional' })`). Critère d'acceptation ajouté à **[SPRINT-PASS-2026-05.md § 5](docs/produit/SPRINT-PASS-2026-05.md)** : *« le PM affiche un dialog d'enregistrement à l'enrôlement, et propose la passkey au focus du champ email au login suivant »*. Phase « WebAuthn comme déverrouillage du coffre Pass lui-même » reste en L3 — c'est un sujet distinct (chiffrement client) traité après la migration.

### 2026-05-13 (J1 sprint Pass — soir) — **Procédure de rollback prod § 10 bis livrée** dans `docs/operations/DEPLOIEMENT-VPS-PORTAINER-NPM.md` (item 3 de la liste « 5 chantiers infra » — les 4 autres restent **gelés** post-20 mai pour ne pas mettre en péril le sprint Pass). Procédure couvre : (10 bis.1) préparer chaque release (snapshot Postgres + volume `cloudity_auth_keys` + futur `cloudity_pass_data` + export Proton offline) ; (10 bis.2) **Cas A** rollback applicatif via TAG_PREVIOUS Portainer (~2-3 min/stack) ; (10 bis.3) **Cas B** rollback DB après migration cassée (`make restore` + redémarrage cascadé) ; (10 bis.4) **Cas C — spécifique migration Proton 20 mai** : matrice de décision selon gravité (item isolé / vault complet / total) + retour Proton Pass possible jusqu'au 25 mai, **ne PAS résilier l'abonnement Proton avant le 25** ; (10 bis.5) **Cas D** rollback NPM/TLS ; (10 bis.6) smoke post-rollback obligatoire (`make smoke-prod` + lecture 1 item Pass test) ; (10 bis.7) post-mortem dans `docs/operations/incidents/YYYY-MM-DD-*.md`. Index `docs/README.md` mis à jour.

**Reportés post-20 mai (4 items « infra durcissement »)** : (1) `docs/cloudity-api-contracts/` (OpenAPI par service — Phase 0 multi-repo, multi-repo gelé), (2) split Portainer en `compose/identity.yml` + `compose/mail.yml`+ etc., (4) **mTLS strict** (`MTLS_MODE=strict` mail-directory puis cascade), (5) Postgres `sslmode=verify-full` + Redis `--tls-auth-clients yes`. Coût total estimé ~5,5 j et risque de régression élevé sur (4)/(5) → ne pas démarrer pendant le sprint Pass.

### 2026-05-13 (J1 sprint Pass) — **`@cloudity/pass-crypto` v0.1.0 livré** (J1 sur le calendrier sprint 13 → 20 mai). Package TS sous `frontend/packages/pass-crypto/` (workspace npm) implémentant le format **EnvelopeV1** spec PASS-CRYPTO § 4 : Argon2id (via `hash-wasm`, profils `desktop` / `mobile-high` / `mobile-low`), **XChaCha20-Poly1305** (`@noble/ciphers`), **HKDF-SHA-256** (`@noble/hashes`), CBOR (`cbor-x`), base64url maison sans padding, hiérarchie `MK → VK → IK_item` avec wipe RAM systématique. **18 tests verts** (`vitest run`, 221 ms, 4 fichiers : round-trip multi-paramètres, anti-tampering AEAD `ct`/`wrap`/`aad`/`nonce_c` + multi-positions, base64url fuzz, générateur uniforme RFC-4648). `tsc --noEmit` strict OK. Dépendance déclarée dans `frontend/apps/cloudity-web/package.json` (`file:../../packages/pass-crypto`) — UI Pass refactor (J3) commencera par déverrouillage maître + éditeur login. **KEM hybride PQ ML-KEM-768 reporté en v0.2** (le format `EnvelopeV1` réserve déjà le champ `kem`, lazy-migration future possible). **Mobile Flutter Pass** repassé en L1 du sprint (lecture seule au 20 mai, édition complète J+1..J+5).

### 2026-05-13 — **Sprint Pass + gel multi-repo**. Décision produit : **priorité absolue** au chemin critique **Pass** (crypto client `EnvelopeV1`, UI Pass complète, import Proton JSON, TOTP item, 2FA compte Cloudity + codes de récupération) pour une **migration utilisable ~20 mai 2026** (marge avant fin abonnement Proton ~25 mai). **Extension navigateur MV3** repoussée en L2 (J+1..J+5) ; **mobile Flutter `pass` LECTURE SEULE en L1** (au 20 mai, édition mobile J+1..J+5). **Scission du monorepo en plusieurs dépôts GitHub** : **gelée** jusqu’à fin du sprint — le dépôt unique reste la référence. Fiche **[docs/produit/SPRINT-PASS-2026-05.md](docs/produit/SPRINT-PASS-2026-05.md)** + section correspondante dans **[BACKLOG.md](BACKLOG.md)**.

### 2026-05-12 — **Anonymisation des fiches déploiement (placeholders neutres)**. `DEPLOIEMENT-VPS-PORTAINER-NPM.md`, `QUESTIONNAIRE.md`, `REPONSES.md`, `REVERSE-PROXY.md`, `STATUS.md`, `BACKLOG.md` : remplacement systématique des données identifiantes (TLD perso, hostname NPM, owner registry, noms des autres applications du VPS) par des placeholders standards (`<DOMAIN>`, `<NPM_HOST>`, `<REGISTRY_OWNER>`, `<EDGE_NETWORK>`, `<INTERNAL_NETWORK>`, `<TAG>`). Section 0 « Variables à substituer » ajoutée en tête de la fiche déploiement (mapping placeholder ↔ valeur réelle, à reporter dans Portainer Stack Variables ou `.env.deploy.local` git-ignored). Bascule des images de `<dockerhub-user>/cloudity-*` vers `ghcr.io/<REGISTRY_OWNER>/cloudity-*` (cohérence Q21=B). Aucune perte de logique : tous les renvois fonctionnels (Q21–Q24, NPM, mTLS, smoke) sont conservés. `gitleaks protect --staged` à 0 fuite.

### 2026-05-12 — **REVERSE-PROXY.md § 4 bis nginx-proxy-manager**. Section dédiée au scénario VPS actuel (NPM partagé `<NPM_HOST>`) : table des 3 Proxy Hosts (`api.` / `app.` / `admin.cloudity.<DOMAIN>`), blocs « Advanced » prêts à coller (HSTS preload, nosniff, X-Frame-Options, Permissions-Policy, COOP/CORP, CSP API stricte / front avec `connect-src` API, rate limit edge, cache long `/assets/*`), table des limites NPM vs Caddy/nginx natif (HTTP/3 Q18=A et PQ X25519MLKEM768 Q19=A à activer après bascule Caddy planifiée), tests rapides via `make smoke-prod`. Aligné Q21–Q24.

### 2026-05-12 — **`make smoke-prod` post-déploiement**. `scripts/ops/smoke-prod.sh` : `GET /health` (200) + `GET /auth/validate` sans Bearer (401) + front SPA (200) + TLS handshake (proto/cipher) + headers durcis (HSTS + nosniff). Authentifié optionnel via `SMOKE_USER`/`SMOKE_PASS` (login → 4 endpoints applicatifs). Aucun secret embarqué. Cible `make smoke-prod` (`SMOKE_API_URL=https://api.cloudity.<DOMAIN> make smoke-prod`).

### 2026-05-12 — **Volume `cloudity_auth_keys` persistant (prod)**. `auth-service` accepte `AUTH_KEYS_DIR` (helper `keyDir()` + `keyPath()`) ; les paires Ed25519 (clé courante) + RSA (rétroAcompat RS256) sont écrites/relues dans ce répertoire. `docker-compose.prod.yml` monte le volume nommé `cloudity_auth_keys` à `/var/lib/cloudity/auth-keys` côté auth-service (RW) et côté api-gateway en RO (`JWT_PUBLIC_KEY_PATH` + `JWT_ED25519_PUBLIC_KEY_PATH`). Plus aucun JWT cassé au redéploiement. Tests `TestKeyDirOverrideWritesAndReloadsEd25519` / `…RSA` / `TestKeyPathCreatesDirectory` ✅ (suite auth-service 2.8 s).

### 2026-05-12 — **CI `check-versioning` livré (Phase 0)**. `scripts/ci/check-versioning.sh` couvre les 4 libs partagées (`internalsec`, `pkg/dbpin`, `@cloudity/shared`, `cloudity_shared`). Détecte tout changement de source non accompagné d'un bump CHANGELOG / version (union diff `merge-base...HEAD` + index + working tree). Modes WARNING (défaut) / BLOCKING (`CHECK_VERSIONING_BLOCKING=1`). Cible **`make check-versioning`** + intégration **`make test-security`** (rapport `reports/security-check-versioning.txt`). Doc **[docs/architecture/VERSIONNAGE-LIBS.md § 6](docs/architecture/VERSIONNAGE-LIBS.md)**.

### 2026-05-12 — **Bloc 4 questionnaire VPS acté (Q21–Q24)**. **Q21=B** (GHCR) et **Q24=A** (GHA matrice) déjà livrés au commit `349d1642` ; **Q22=A** (réutiliser le bridge external déjà branché à NPM, `<EDGE_NETWORK>`) et **Q23=A** (`cloudity.<DOMAIN>` + `api.` / `admin.`) actés comme défaut. Détails + conséquences DNS / Portainer / NPM dans **[docs/decisions/multi-repo/REPONSES.md](docs/decisions/multi-repo/REPONSES.md) bloc 4**. Tests Go non-régression OK (auth-service 2.0 s, api-gateway 22.6 s, internalsec, mail-directory) ; ESLint cloudity-web clean.

### 2026-05-12 — **E2E Playwright WebAuthn (CDP)**. `e2e/webauthn.spec.ts` + `e2e/fixtures/webauthn-virtual.ts` (`WebAuthn.addVirtualAuthenticator`, ctap2 / internal / resident key). Smoke bouton passkey sur `/login` ; flux admin : mot de passe → `/4dm1n/passkeys` → enregistrement → déconnexion → login passkey. Cible **`make test-e2e-playwright-webauthn`** (prérequis : **`make migrate`** migration **37**, **`make seed-admin`**). Skip si **`GET /health`** sur le dashboard échoue. Doc **TESTS.md**, **WEBAUTHN-PLAN.md** § 7, **BACKLOG**.

### 2026-05-12 nuit octies)** — **WebAuthn Phase W2 frontend livré**. Module `frontend/apps/cloudity-web/src/webauthn.ts` (encodage base64url ↔ ArrayBuffer, helpers `registerPasskey`, `loginWithPasskey`, `listPasskeys`, `deletePasskey`). Page `/4dm1n/passkeys` (liste + ajout + suppression avec confirmation, React Query + toast + lucide). Bouton "Se connecter avec une passkey" sur `LoginPage` (visible uniquement si `isWebAuthnSupported()`). Backend : nouveaux endpoints `GET /auth/webauthn/credentials` + `DELETE /auth/webauthn/credentials/:id` (suppression scoppée user_id). Vitest 246 ✅, build Vite OK, ESLint OK, Go ✅.

### 2026-05-12 nuit septies)** — **mTLS gateway↔auth-service livré (Go + step-ca)**. `auth-service` import `internalsec`, démarre via `http.Server.ListenAndServeTLS` quand `MTLS_MODE != off` (mode `permissive` actuellement). `go.mod` updated avec `replace ../internalsec`. `docker-compose.https.yml` : bind-mount certs `auth-service/{cert,key,ca}.pem` step-ca + séparation CA Postgres (`/run/step/postgres-ca.pem`) / CA Redis (`/run/step/redis-ca.pem`) pour ne pas écraser `/run/step/ca.pem` qui sert maintenant le serveur TLS. `AUTH_SERVICE_URL=https://auth-service:8081` côté gateway. Test unitaire `TestAuthServiceListensInTLSWhenMTLSPermissive` : handshake bout-en-bout via certs ECDSA P256 auto-signés locaux, RoundTripper `internalsec` côté client, status `200 /health`. `make mtls-issue-auth` (auto via `up-https-internal`). Tests Go ✅. Voir **[docs/securite/AUDIT-SECURITE.md](docs/securite/AUDIT-SECURITE.md)** § 6 bis.

### 2026-05-12 nuit sexies)** — **sidecar `cert-renewer` livré (rotation auto certs internes)**. Service Docker dans `docker-compose.security.yml` qui boucle `step ca renew --expires-in 6h` toutes les 10 min sur tous les certs `infrastructure/step-ca/issued/<svc>/`. Démarré automatiquement par `make mtls-up`. Aucun mot de passe CA sur disque (auth via cert+clé existants). Cibles `make cert-renewer-status` / `cert-renewer-restart`. Renew effectif vérifié manuellement (notBefore 17:13 → 19:41 sur api-gateway). Hygiène secrets : balayage `.md` complet → mots de passe de démo retirés de STATUS/BACKLOG/mobile/photos/README ; gitleaks Git history = 0 fuite. Voir **[docs/securite/MTLS-INTERNE.md § 3.4](docs/securite/MTLS-INTERNE.md)**.

### 2026-05-12 nuit quinquies)** — **WebAuthn Phase W1 backend livré (Q17=A)**. Endpoints `/auth/webauthn/{register,login}/{begin,finish}` opérationnels via lib `go-webauthn/webauthn v0.17.3`. Migration `37-webauthn-credentials.sql` (FK `users(id)`, unique global `credential_id`, replay protection via `sign_count` monotone). Challenges en Redis (TTL 5 min, usage unique). Tests unitaires Go ✅ (config + JWT admin gate). Gateway : `/auth/webauthn/login/*` ouvert en public, `register/*` protégé Bearer admin. Variables `WEBAUTHN_RP_ID`, `WEBAUTHN_RP_NAME`, `WEBAUTHN_ORIGINS`.

### 2026-05-12 nuit quater)** — **`Dockerfile.prod` + GHA `docker-publish.yml` livrés (Q24=A)**. Images multi-stage publiées sur **GHCR** à chaque push `main`/`master` ou tag `v*.*.*` :

- 9 services Go → `gcr.io/distroless/static-debian12:nonroot` (build `-trimpath -ldflags="-s -w" -buildvcs=false`).
- `api-gateway` build depuis `backend/` (replace `../internalsec`) avec `Dockerfile.prod` dédié.
- `admin-service` (Python) → `python:3.11-slim`, user `cloudity` uid 1000, `libpq5` only.
- `frontend` (cloudity-web) → image existante (Dockerfile multi-stage Vite + nginx).

Builds locaux validés : `auth-service` (boot Gin OK :8081), `api-gateway`, `passwords-service`, `admin-service`. Workflow YAML linté. Voir **[docs/operations/DEPLOIEMENT-VPS-PORTAINER-NPM.md § 9](docs/operations/DEPLOIEMENT-VPS-PORTAINER-NPM.md)**.

### 2026-05-12 nuit ter)** — **mTLS gateway↔admin-service livré (PoC fonctionnel)** : `internalsec.InternalRoundTripper(ConfigFromEnv())` câblé dans `httputil.ReverseProxy.Transport` (api-gateway). `admin-service` (uvicorn) démarre via **`backend/admin-service/start.sh`** qui passe automatiquement en `--ssl-keyfile/--ssl-certfile/--ssl-ca-certs/--ssl-cert-reqs` quand `MTLS_MODE=permissive|strict`. **`make up-https-internal`** émet désormais aussi `api-gateway` + `admin-service` certs via **`make mtls-issue-admin`**. Variables `AUTH_SERVICE_URL`, `ADMIN_SERVICE_URL`, etc. désormais surchargeables (gateway), pour basculer chaque lien séparément. Tests Go gateway/internalsec/auth/mail-directory ✅. Voir **[docs/securite/AUDIT-SECURITE.md](docs/securite/AUDIT-SECURITE.md)** § 6 bis.

### 2026-05-12 nuit bis)** — **HTTPS partout (cible Zero Trust)** : nouvelles cibles **`make up-tls`** (edge Caddy TLS 1.3 + HSTS + CSP par défaut, hybride PQ X25519MLKEM768 quand dispo) et **`make up-https-internal`** (Postgres TLS `sslmode=verify-ca` + Redis `--tls-port` + **auth-service** `REDIS_TLS=1` / go-redis `TLSConfig`). Override **`docker-compose.https.yml`** + **`make mtls-chown-internal-certs`** (bind-mount PEM avec uid postgres **70** / redis **999**). Cibles `mtls-issue-postgres` / `mtls-issue-redis` (TTL **24 h** max provisioner par défaut) + `make https-status`. Variables `MTLS_*` / `REDIS_TLS*` dans `.env.example`. Plan dans **[docs/securite/AUDIT-SECURITE.md](docs/securite/AUDIT-SECURITE.md)** § 6 bis.

### 2026-05-12 nuit)** : **PoC mTLS step-ca livrable** : `make mtls-poc` (`mtls-up` + `seed-mtls` + `mtls-issue NAME=…` + `mtls-verify`) émet un cert client/serveur 24 h avec SAN SPIFFE `spiffe://cloudity.local/ns/default/sa/<svc>` ; chaîne CA bundle (root + intermediate) → `openssl verify` OK ; **test d'intégration Go `TestStepCAIssuedCertsHandshake`** (gated `INTERNALSEC_STEPCA_INTEGRATION=1`) prouve un handshake TLS 1.3 + `RequireServiceCallerHTTP` avec les certs **réels** émis par step-ca. Voir **[docs/securite/MTLS-INTERNE.md](docs/securite/MTLS-INTERNE.md)** § 3.3.b.

### 2026-05-12 soir bis — **politique secrets blindée** : nouveau guide **[docs/securite/SECRETS.md](docs/securite/SECRETS.md)** (inventaire, génération, rotation, gitleaks, procédure incident) ; `.gitignore` durci (`.env.*`, `*.pem`, `*.key`, `step-ca/secrets/*`, dumps SQL) ; **`MAIL_PASSWORD_ENCRYPTION_KEY` placeholder hex remplacé par 64 zéros** (anciennement `a1b2c3…67890`, vraie clé AES-256) — `mail-directory-service` refuse de chiffrer/déchiffrer avec la clé zéros (`validateMailEncryptionKeyAtBoot` + tests `TestEncryptPasswordRejectsZeroKey`/`TestIsZeroHexKey`) ; **`gitleaks` intégré à `make test-security`** (157 commits scannés, 0 fuite) — cibles utilitaires `make secrets-scan` / `make secrets-scan-staged`.

### 2026-05-12 soir — **mail admin-only Zero Trust** : **double contrôle** gateway (JWT + rôle admin + `Origin`) **et** `mail-directory-service` (`X-Admin-Role: admin` requis) ; gateway **strippe** `X-User-ID`/`X-Tenant-ID`/`X-Admin-Role` à l'entrée (`stripInternalTrustHeaders`). **HTTPS dev** : **`make dev-https`** (mkcert + Vite). **Helper secrets** : **`make secrets`** génère POSTGRES/REDIS/JWT/`PERFORMANCE_INGEST_TOKEN` en 256 bits. Tests Go gateway + mail-directory-service ✅. Bloc Q25–Q31 dans **QUESTIONNAIRE / REPONSES**.

### 2026-05-12 matin — durcissement **admin** : UI **`/admin*`** refusée (pas de redirection vers `/4dm1n`) ; gateway **`/admin/*`** exige **JWT admin + `Origin`** ; `POST /admin/performance/pipeline-run` exige **JWT + `X-Cloudity-Perf-Ingest`** (`PERFORMANCE_INGEST_TOKEN` **obligatoire** gateway+admin-service) ; restauration des guides `docs/{architecture,operations,produit,securite}` + audit renommé **`docs/securite/AUDIT-SECURITE.md`**. *(Historique ci-dessous.)*

### (historique) 2026-05-05 (convention doc : racine **README** + **STATUS** + **BACKLOG.md** ; ROADMAP / TESTS / MOBILES / PlanImplementation / guides → **`docs/`**). **§0b A1** : workspaces npm racine **`frontend/`** (détail tableau ci-dessous). **Mail** : barre de page allégée (**Nouveau message** seul) ; comptes / Google / paramètres app / synchro dans **« Menu Mail »** à côté du fil d’Ariane (**`AppPageChromeContext`** — réutilisable Calendar, etc.). ### Mise à jour mai 2026** : **bridge AppPageChrome** (setters / affichage + **`MailAppChromeMenu`**) ; **`h1` document « Mail »** (`sr-only`, a11y + E2E) ; **Playwright** **`make test-e2e-playwright-mail`** (désormais 8 scénarios, dont navigation Mail ↔ Drive + règles combinées + rétro-application) ; **Vitest / ESLint** dashboard via **Docker** (`make test-dashboard*`). Logs gateway **`JWT … expired`** sur syncs parallèles : bruit connu si rafraîchissement token en cours (voir **TESTS.md** § 4.8). **Suite avril** : suppression en masse corbeille, favicons proxy backend, réconciliation IMAP après règles, anti-rafale sync UI, **polling ~12 s** page Mail / **~18 s** hors Mail, sync après envoi, indicateur sync sidebar. **Migrations** : voir tableau §0 (**`make migrate`**) et **TESTS.md** / **SYNC-BACKLOG §0d**. **Performances** : **[docs/operations/PERFORMANCES.md](docs/operations/PERFORMANCES.md)** + **BACKLOG** n°9 + **ROADMAP TR-06** ; gros exports de trace ignorés par **`.gitignore`**. **Nouveau** : opérateurs de recherche Mail (`from:`, `subject:`, `tag:`) + tests Vitest dédiés ; migration contrôlée **`vite@8` / `vitest@4`** ; remédiation `xlsx` par remplacement de librairies Office (lecture/écriture `.xlsx`) et **audit npm dashboard à 0 vulnérabilité** ; tests mobile : option **`CLOUDITY_KEEP_APP_OPEN=1`** pour relancer et garder l’app ouverte après `integration_test`.  

### Mise à jour 2026-05-06 (en cours)** : Mail web — suppression de boutons en doublon dans la sidebar (actions déjà présentes dans `Menu Mail`), popups `Paramètres Mail`/`Ajouter une boîte` fermables au clic extérieur, compose enrichi (formatage HTML de base), transfert renommé `Transférer le message` avec contenu transféré HTML, programmation d’envoi via modale interne (fin de `window.prompt`) ; gateway — verrouillage admin-only des routes `/mail/domains*`, `/mail/mailboxes*`, `/mail/aliases*` + tests Go dédiés.  
### Mise à jour 2026-05-06 (performance / admin)** : ajout d’un endpoint admin **`/admin/performance/overview`** (snapshot runtime cgroup + docker stats si disponibles) et affichage dans le dashboard admin ; base de traçabilité posée pour la phase suivante (historisation multi-services + métriques de runs tests/E2E/mobile).  
**Branches** : **`main`** = stable ; **`dev`** = intégration ; **`feat/<sujet>`** = chantiers (ex. `feat/photos-gallery-mobile-sync-security` pour Photos galerie + mobile + sync + durcissement sécurité). Détail : **[docs/operations/BRANCHES.md](docs/operations/BRANCHES.md)**.  
