#!/usr/bin/env bash
# Stub : remise d’un message reçu sur *@<DOMAINE-ALIAS> vers la boîte IMAP cible.
# Brancher sur l’API mail-directory-service (lookup alias) puis lmtp/smtp vers le fournisseur.
# Ne pas committer d’adresses réelles.
set -euo pipefail
echo "alias-deliver: stub — brancher lookup Cloudity + injection IMAP" >&2
exit 75
