# Coller dans NPM → Proxy Host WEB → Advanced (cloudity + sous-domaines apps)
# Forward : cloudity-web:80 · SSL : Let's Encrypt + Force SSL

# Redirections directes vers l'app (bookmarks sous-domaines)
if ($host = "mail.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/mail$is_args$args;
}
if ($host = "drive.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/drive$is_args$args;
}
if ($host = "pass.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/pass$is_args$args;
}
if ($host = "calendar.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/calendar$is_args$args;
}
if ($host = "notes.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/notes$is_args$args;
}
if ($host = "tasks.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/tasks$is_args$args;
}
if ($host = "contacts.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/contacts$is_args$args;
}
if ($host = "photos.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/photos$is_args$args;
}
if ($host = "office.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/app/office$is_args$args;
}
if ($host = "admin.cloudity.delhomme.ovh") {
  return 302 https://cloudity.delhomme.ovh/4dm1n$is_args$args;
}

# mails → mail
if ($host = "mails.cloudity.delhomme.ovh") {
  return 301 https://mail.cloudity.delhomme.ovh$request_uri;
}
