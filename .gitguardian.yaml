# GitGuardian — faux positifs documentés (cf. docs/securite/SECRETS.md § GitGuardian).
# Les mots de passe démo ne doivent plus être en dur dans le code mobile (dart-define + .env).

paths-ignore:
  - name: Go tests — credentials démo seed-admin
    paths:
      - backend/**/**_test.go
  - name: E2E Playwright — env PLAYWRIGHT_E2E_PASSWORD
    paths:
      - frontend/apps/cloudity-web/e2e/**
